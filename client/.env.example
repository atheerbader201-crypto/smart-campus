# Local dev — Vite (5173) talks to API on 5000
VITE_API_URL=http://localhost:5000

# Production (Docker on Render, same URL for UI + API): leave empty or omit
# VITE_API_URL=

# Split deploy (static site + separate API service only):
# VITE_API_URL=https://your-api-name.onrender.com
