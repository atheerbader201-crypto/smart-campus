# Copy to server/.env — paste your real string from MongoDB Atlas (Connect → Drivers).
# Host must look like cluster0.xxxxx.mongodb.net (not cluster.... or cluster.mongodb.net).
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/SmartCampus?retryWrites=true&w=majority
PORT=5000
